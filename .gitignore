# macOS
.DS_Store

# Local tool settings
.claude/settings.local.json

# Mood-board references — third-party screenshots, not used by the site.
# Kept locally, deliberately not published.
assets/*.jpeg
assets/*.jpg
assets/*.png

# Duplicate of assets/Kristina-Zakharchenko-CV.docx (the copy the site actually links to)
/CV Kristina Zakharchenko _ Growth & Performance Marketing Manager.docx

# ---------------------------------------------------------------------------
# NEVER PUBLISH. This repo deploys to a PUBLIC website — anything committed
# here is world-readable at kristyzakh.github.io. These are tax registration
# documents, residence records, signed contracts, client invoices and the
# generator that embeds bank details.
# ---------------------------------------------------------------------------
legacy/
legal/
